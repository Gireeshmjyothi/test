@Query(value = """ 
    SELECT map.*, maf.RFS_ID FROM
    MERCHANT_ACCOUNT_PAYOUT map,
    MERCHANT_ACCOUNT_FILE_WISE_PAYOUT maf
    WHERE map.MAP_ID = maf.MAP_ID AND map.STATUS = :status
    """, nativeQuery = true)
